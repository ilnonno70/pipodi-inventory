services: docker

script:
  - docker build -t pipodi/inventory-hunter .

deploy:
  provider: script
  script: echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin && docker push pipodi/inventory-hunter
  on:
    branch: main